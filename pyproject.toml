[project]
name = "mcp-runner"
version = "0.1.0"
description = "MCP Runner Template"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
