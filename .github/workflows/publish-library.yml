name: Publish Library

on:
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to build'
        required: true
        default: 'tts_mcp'
        type: choice
        options:
          - tts_mcp

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build and publish
        run: |
          cd ${{ inputs.service }}
          pip install build twine
          python -m build
          python -m twine upload dist/* --repository-url https://maven.pkg.github.com/${{ github.repository_owner }} -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }}
        env:
          # UV_PUBLISH_TOKEN is not needed for twine
          TWINE_USERNAME: ${{ github.actor }}
          TWINE_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
