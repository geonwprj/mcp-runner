name: Publish Library

on:
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to build'
        required: true
        default: 'tts_mcp'
        type: choice
        options:
          - tts_mcp

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build and publish
        run: |
          cd ${{ inputs.service }}
          pip install build
          python -m build
          
          # Extract version from pyproject.toml
          VERSION=$(grep -m1 'version =' pyproject.toml | cut -d '"' -f2)
          TAG="v$VERSION"
          
          # Create release and upload assets
          # If release exists, this might fail, so we can use '|| true' or check first.
          # For simplicity in this flow, we attempt to create.
          gh release create "$TAG" dist/* --title "$TAG" --generate-notes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
